"""LangGraph wrapper for the future sample-diagnosis core implementation."""

from __future__ import annotations

from typing import Any


def run(state: dict[str, Any]) -> dict[str, Any]:
    """样本诊断节点占位入口；完整实现将在后续 LangGraph 节点接入。"""
    raise NotImplementedError("Sample-diagnosis core is the next implementation stage")
