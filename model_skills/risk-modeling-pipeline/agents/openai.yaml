interface:
  display_name: "风控自动建模"
  short_description: "用户确认后执行 AI 辅助风控 EDA、LightGBM 调参与审查"
  default_prompt: "使用 $risk-modeling-pipeline 按读取数据、EDA分析、样本诊断、特征预处理筛选、模型配置、训练调参、模型审查、报告交付八个主节点处理我的结构化风控数据；每个确认门禁先反馈证据并等待我确认，再运行可复现的 EDA、Optuna-LightGBM 建模与红队审查。"
