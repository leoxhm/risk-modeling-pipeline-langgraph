# 特征预处理与筛选完成

> 使用 workflow 返回的 `summary` 原文渲染为 Markdown，不要把 YAML 配置或完整 CSV 内容复制到聊天区。

## 结果摘要

| 类别 | 字段/指标 | 当前结果 | 说明 |
|---|---|---|---|
| 数据概况 | 输入规模 | `<行数>` 行 × `<候选特征数>` 个候选特征 | 按已确认的 data-read 角色重新读取 |
| 字段去留 | 直接保留 | `<数量>` 个；`<字段预览>` | 通过缺失率、IV、相关性和稳定性规则 |
| 字段去留 | 待复核（暂保留） | `<数量>` 个；`<字段预览>` | 存在稳定性或集中度提示 |
| 字段去留 | 规则剔除 | `<数量>` 个；`<字段>`（`<实际原因>`） | 只展示前 8 个；过多时标明总数，完整清单见 `feature_selection_summary.md` |
| 字段去留 | 变量类型剔除 | `<数量>` 个；`<字段预览>` | 按文本、高基数、近似主键等预处理策略剔除 |
| 处理结果 | 输出数据 | `<行数>` 行 × `<列数>` 列 | 已生成 `processed_data.parquet` |
| 筛选阈值 | 缺失率 / IV / PSI / 相关系数 | `<实际阈值>` | 本次实际执行的 YAML 参数 |
| 实际指标 | IV / KS / 最大月度 PSI | `<范围与均值>` | 完整字段级值见 `feature_statistics.csv` |
| 预处理策略 | 数值 / 类别 / 文本 | `<实际策略>` | 说明缺失、编码、高基数和文本处理方式 |

## 反馈规则

- 结论只来自本次 workflow 产物；告警是“需关注”，不自动阻断建模。
- 用户确认结果后才能进入下一个已选节点。
- 用户提出调整时，只修改当前节点 YAML 并重跑当前节点，不修改 Skill 源码。
